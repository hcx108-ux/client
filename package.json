{
  "name": "figma-akashvani-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix client",
    "dev": "npm run dev --prefix client",
    "install:client": "npm install --prefix client"
  }
}
